extends layout
block content
  head 
    link(rel='stylesheet', href='css/dm.css')
  body.dm
    h1.dm= 'error :('
    h2.dm= `${error.status == '404' ? '404, nothing found here' : error.status ? error.status : '' + ' ' + message}`
    pre.dm #{error.stack}
